# Copy this file to .env.local for local development, and add the same
# variable in your Vercel/Cloudflare Pages project settings for production.

# Server-only Web3Forms access key — never prefix with NEXT_PUBLIC_.
# The contact API route reads this; it is not sent to the browser.
WEB3FORMS_KEY=
